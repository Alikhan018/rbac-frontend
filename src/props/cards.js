import {
  faUser,
  faUserGroup,
  faGear,
} from "@fortawesome/free-solid-svg-icons";

const users = {
  icon: faUser,
  name: "Users",
};

const groups = {
  icon: faUserGroup,
  name: "Groups",
};

const roles = {
  icon: faGear,
  name: "Roles",
};

export { users, groups, roles };
